import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://skcehyqjhjzqtafvuvye.supabase.co', 'sb_publishable_gyoZ1eM6w6WU9w3dteKoug_Ua-a3Dke'); async function run() { const {data: auth} = await supabase.auth.signInWithPassword({ email: 'lucas.soares@estoquefacil.local', password: 'Trocar@123' }); console.log(auth.session ? 'Logged in' : 'Failed login'); const {data, error} = await supabase.from('companies').insert([{name: 'Test', slug: 'test-slug-1234'}]).select().single(); console.log(error || data); } run();
